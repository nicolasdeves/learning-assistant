import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  title: {
    fontSize: 20,
    color: '#333333',
    marginLeft: 30,
    fontFamily: 'sans-serif-light',
  },

  topics: {
    margin: 30,
    marginBottom: 100
  }
});
